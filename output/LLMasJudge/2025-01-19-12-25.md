## verifying challenge mychallenges/c002.md 

### compile try: 1
#### STUDENT ANSWER:
 ```envision
table T = with
  [| as N |]
  [| 1 |] // line 1
  [| 2 |] // line 2
  [| 3 |] // line 3

where T.N < 3
  show table "Filtered Table" a1b3 with
    T.N
```
#### Compilation: OK
### JUDGE ANSWER:
 The professor's answer provides a clear guideline on how to create a table, apply a filter, and display the filtered content. The key points to note are the indentation of the `show table` command after the `where` statement and the correct usage of the `show table` command.

Now, let's evaluate the student's answer based on the provided documentation:

1. The student creates a table `T` with 3 lines and defines a column `N`. This is ACCEPTABLE.
   
2. The student applies a filter `where T.N < 3` to keep lines where `N` is less than 3. This is ACCEPTABLE.

3. The student uses the `show table` command to display the filtered content. However, the indentation of the `show table` command is incorrect. It should be indented with 2 whitespaces after the `where` statement. This makes the student's answer UNACCEPTABLE.

4. The student correctly specifies the table name as "Filtered Table" and selects the column `T.N` to display. This is ACCEPTABLE.

Based on the evaluation, the student's answer is mostly correct but the incorrect indentation of the `show table` command makes it UNACCEPTABLE.

Therefore, the final judgment is 0.
### judge_decision: False


## Compilation success rate:
1 out of 1, 100.0%
## Accuracy:
0 out of 1, 0.0%
