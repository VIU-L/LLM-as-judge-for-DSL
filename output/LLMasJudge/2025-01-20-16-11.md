## verifying challenge mychallenges/c000.md 

### compile try: 1
#### STUDENT ANSWER:
 ```envision
greetings = "Hello" 
show label greetings
```
#### Compilation: OK
### JUDGE ANSWER:
 The student's answer is **ACCEPTABLE**.
- The student correctly defined a text literal called `greetings` with the value "Hello".
- The student correctly displayed `greetings` on the dashboard as a label.
  
Therefore, the student's answer fulfills the requirements of the question without any issues. 
### Final Judgment: 1
### judge_decision: True


## verifying challenge mychallenges/c001.md 

### compile try: 1
#### STUDENT ANSWER:
 ```envision
table T = with
  [| as X |]
  [| "a" |]
  [| "b" |]
  [| "c" |]

show table "My Table" a1b3 with
  T.X
```
#### Compilation: OK
### JUDGE ANSWER:
 The professor's answer creates a table `T` with a column `X` containing the values `a`, `b`, and `c`, and then displays this table with the column `X`. The student's answer also creates a table `T` with the same structure and displays it with the column `X`. The only difference is that in the `show` command, the student added `a1b3` which is an extra print position compared to the professor's answer. This difference does not affect the logic of the answer.

Therefore, the student's answer is **ACCEPTABLE**. 

The final judgment is 1.
### judge_decision: True


## verifying challenge mychallenges/c002.md 

### compile try: 1
#### STUDENT ANSWER:
 ```envision
table T = with
  [| as N |]
  [| 1 |] // line 1
  [| 2 |] // line 2
  [| 3 |] // line 3

// Filtered tables: `T`
where T.N > 1 // lines left in `T`: 2, 3
  show table "Filtered T" a1b2 with
    T.N // Displaying the content of the filtered table
```
#### Compilation: OK
