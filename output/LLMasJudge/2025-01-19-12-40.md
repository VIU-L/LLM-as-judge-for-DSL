## verifying challenge mychallenges/c002.md 

### compile try: 1
#### STUDENT ANSWER:
 ```envision
table T = with
  [| as N |]
  [| 1 |] // line 1
  [| 2 |] // line 2
  [| 3 |] // line 3

// Filtered table: `T`
where T.N > 1 // lines left in `T`: 2, 3
  show scalar "T" a1 with count(T.*) // `2`
  show scalar "T" a2 with T.N // `2`, `3`
```
#### Compilation Failed
Error: Cannot broadcast T data to Scalar data. (Line: 10, Start: 27, Length: 3, Severity: Error)
### compile try: 2
#### STUDENT ANSWER:
 ```envision
table T = with
  [| as N |]
  [| 1 |] // line 1
  [| 2 |] // line 2
  [| 3 |] // line 3

where T.N > 1
  show table "Filtered Table" a1b2 with
    T.N
```
#### Compilation: OK
### JUDGE ANSWER:
 The student's answer is mostly acceptable. Here is a step-by-step evaluation:

1. The student correctly creates a table `T` with 3 lines and a column `N`. - ACCEPTABLE
2. The student correctly applies a filter to keep only lines where `N` is greater than 1. - ACCEPTABLE
3. The student uses the `show table` command to display the filtered table. - ACCEPTABLE
4. The student correctly specifies the column `N` to be displayed in the filtered table. - ACCEPTABLE
5. The student uses the correct table name "Filtered Table" in the `show table` command. - ACCEPTABLE
6. The student correctly uses the `a1b2` position to display the content of the filtered table. - ACCEPTABLE
7. The student does not indent the `show table` command with 2 whitespaces after the `where` statement, as required by the professor's answer. - UNACCEPTABLE

Overall, the student's answer is ALMOST COMPLETE, but it is UNACCEPTABLE due to the missing indentation in the `show table` command. Therefore, the final judgment is 0.
### judge_decision: False


## verifying challenge mychallenges/c003.md 

### compile try: 1
#### STUDENT ANSWER:
 ```envision
table Orders = with
  [| as Sku, as Qty, as UnitPrice |]
  [| "a",    5     , 1.5          |]
  [| "b",    3     , 7.0          |]
  [| "b",    1     , 2.0          |]
  [| "c",    7     , 5.7          |]

Orders.InventoryValue = Orders.Qty * Orders.UnitPrice
show scalar "Total Inventory Value" with sum(Orders.InventoryValue)
```
#### Compilation: OK
### JUDGE ANSWER:
 The student's answer is **ACCEPTABLE**.

- The student correctly defines the table `Orders` with columns `Sku`, `Qty`, and `UnitPrice`.
- The student correctly calculates the `InventoryValue` by multiplying `Qty` and `UnitPrice`.
- The student correctly displays the total inventory value using the `sum` function and `show` command.
- The student's code achieves the goal of computing the total inventory value for all SKUs using the price as the value indicator and then displaying the total.

Therefore, the student's answer is logically correct and follows the same approach as the professor's answer. 

The final judgment is 1.
### judge_decision: True


## verifying challenge mychallenges/c004.md 

